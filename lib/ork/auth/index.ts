// @ork/auth — Authentication utilities for ORK
// Import specific exports via subpaths:
//   @ork/auth/context  — useAuth(), useUnit() (Client Component)
//   @ork/auth/server   — requireUser(), getCurrentUser(), requireRole(), isFounder()
//   @ork/auth/unit     — getCurrentUnit()
